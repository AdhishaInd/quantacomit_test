package org.example.remotegalleryproject.entities;

public class ImageData {
    // Give photo list as list of ImageDetails objects

    // Give a photo as byte array

    // Add a photo to uploads directory

    // Delete the list of images given as a list of ImageDetails objects
}
